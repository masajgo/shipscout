[build]
dockerfilePath = "worker/Dockerfile"

[deploy]
startCommand            = "node worker/aisWorker.js"
restartPolicyType       = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath         = ""   # pure background worker, HTTP yok
